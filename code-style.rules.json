{
  "version": 3,
  "guide": "CODE-STYLE.md",
  "rules": [
    {
      "id": "path.capability-layout",
      "statement": "Behavior lives in the named capability directory that owns it.",
      "verify": "pnpm style"
    },
    {
      "id": "path.no-generic-bucket",
      "statement": "Every authored file name states the domain job it performs.",
      "verify": "pnpm style"
    },
    {
      "id": "path.source-directory-case",
      "statement": "Authored source directories use camelCase.",
      "verify": "pnpm style"
    },
    {
      "id": "architecture.no-wrapper-layer",
      "statement": "A repository-owned layer exists only when it owns policy the official service does not.",
      "verify": "judgment"
    },
    {
      "id": "function.arrow-only",
      "statement": "Named functions are arrow constants declared before first use.",
      "verify": "pnpm style"
    },
    {
      "id": "function.effect-generator",
      "statement": "The only generator is an anonymous callback passed directly to `Effect.gen`.",
      "verify": "pnpm style"
    },
    {
      "id": "function.input-shape",
      "statement": "A function takes one cohesive input, or two only as a natural pair.",
      "verify": "pnpm style"
    },
    {
      "id": "function.one-job",
      "statement": "A function performs one job its name fully describes.",
      "verify": "judgment"
    },
    {
      "id": "function.blank-line",
      "statement": "Adjacent function declarations are separated by exactly one blank line.",
      "verify": "pnpm style"
    },
    {
      "id": "function.nesting",
      "statement": "Control flow nests at most two levels deep.",
      "verify": "pnpm style"
    },
    {
      "id": "class.tagged-error-only",
      "statement": "The only authored class directly extends `Schema.TaggedError`.",
      "verify": "pnpm style"
    },
    {
      "id": "comment.loop-intent",
      "statement": "Every explicit loop carries an intent comment on the line directly above it.",
      "verify": "pnpm style"
    },
    {
      "id": "comment.index-proof",
      "statement": "Every indexed non-null access carries a bounds proof comment directly above it.",
      "verify": "pnpm style"
    },
    {
      "id": "comment.pipeline-contract",
      "statement": "A multi-phase pipeline states its contract above the orchestration and numbers each phase inside it.",
      "verify": "judgment"
    },
    {
      "id": "type.schema-owned-runtime",
      "statement": "Data crossing a runtime, persistence, CLI, environment, catalog, or agent-format boundary is defined as Effect Schema first, with its TypeScript type derived.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-handrolled-parser",
      "statement": "Schema owns validation, and no exported type-guard or parse helper duplicates it.",
      "verify": "judgment"
    },
    {
      "id": "type.no-interface",
      "statement": "Interfaces appear only in declaration-file augmentation.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-enum",
      "statement": "Unions derived from schema literals replace enums.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-conditional",
      "statement": "Types are derived from schemas, never computed with conditional or `infer` machinery.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-assertion",
      "statement": "Unknown input is decoded or narrowed, never asserted.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-suppression",
      "statement": "No suppression directive silences a type, lint, format, or coverage error.",
      "verify": "pnpm style"
    },
    {
      "id": "barrel.direct-wildcard",
      "statement": "Every statement in a barrel is a direct wildcard export.",
      "verify": "pnpm style"
    },
    {
      "id": "name.domain-specific",
      "statement": "Every identifier names its domain job or value.",
      "verify": "pnpm style"
    },
    {
      "id": "mutation.no-input",
      "statement": "A function never mutates a value it received.",
      "verify": "pnpm style"
    },
    {
      "id": "collection.no-builder-reduce",
      "statement": "Collections are built with `map`, `filter`, `flatMap`, or `Object.fromEntries`, never `reduce`.",
      "verify": "pnpm style"
    },
    {
      "id": "effect.no-promise-all",
      "statement": "Application code composes concurrency with Effect operators, never `Promise.all`.",
      "verify": "pnpm style"
    },
    {
      "id": "effect.runtime-edge",
      "statement": "Only `src/cli/main.ts` starts the Effect runtime.",
      "verify": "pnpm style"
    },
    {
      "id": "effect.official-services",
      "statement": "Capabilities call official Effect platform services directly.",
      "verify": "judgment"
    },
    {
      "id": "import.hook-runtime",
      "statement": "An installed hook imports only `node:*` builtins, shared `src/runtime/**`, and its own feature runtime.",
      "verify": "pnpm style"
    },
    {
      "id": "import.application-boundary",
      "statement": "Application code never imports installed hook-runtime modules.",
      "verify": "pnpm style"
    },
    {
      "id": "presentation.terminal-ui",
      "statement": "Application code returns structured results and leaves terminal output to `TerminalUI`.",
      "verify": "pnpm style"
    }
  ],
  "protectedPaths": [
    {
      "path": "src/skills/makeATrailer/SKILL.md",
      "codeRuleExemptions": []
    },
    {
      "path": "src/skills/makeATrailer/reference/pipeline.md",
      "codeRuleExemptions": []
    },
    {
      "path": "src/skills/makeATrailer/scripts/assembleCut.mjs",
      "codeRuleExemptions": ["function.arrow-only", "function.input-shape", "comment.loop-intent"]
    }
  ]
}
