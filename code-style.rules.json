{
  "version": 4,
  "guide": "CODE-STYLE.md",
  "rules": [
    {
      "id": "path.capability-layout",
      "statement": "Behavior lives in the named capability directory that owns it.",
      "verify": "pnpm style"
    },
    {
      "id": "path.no-generic-bucket",
      "statement": "Every authored file and directory name states the domain job it performs.",
      "verify": "pnpm style"
    },
    {
      "id": "path.source-directory-case",
      "statement": "Authored source directories use camelCase.",
      "verify": "pnpm style"
    },
    {
      "id": "path.payload-runtime-split",
      "statement": "Executable feature runtime lives under `src/hookIsland/`, never under `src/skills/`.",
      "verify": "pnpm style"
    },
    {
      "id": "architecture.no-wrapper-layer",
      "statement": "A repository-owned layer exists only when it owns policy the official service does not.",
      "verify": "judgment"
    },
    {
      "id": "architecture.policy-mechanism",
      "statement": "Business decisions are pure functions and mutable filesystem, process, and terminal mechanisms are separated by reason to change.",
      "verify": "judgment"
    },
    {
      "id": "architecture.earned-abstraction",
      "statement": "An abstraction exists only when it names a domain concept, owns a side-effect boundary, or serves a second caller.",
      "verify": "judgment"
    },
    {
      "id": "architecture.no-speculation",
      "statement": "Fallbacks, optional branches, and recovery paths exist only at a named trust boundary with a real failure requirement.",
      "verify": "judgment"
    },
    {
      "id": "function.arrow-only",
      "statement": "Named functions are arrow constants declared before first use.",
      "verify": "pnpm style"
    },
    {
      "id": "function.effect-generator",
      "statement": "The only generator is an anonymous callback passed directly to `Effect.gen`.",
      "verify": "pnpm style"
    },
    {
      "id": "function.input-shape",
      "statement": "A function takes one cohesive input, or two only as a natural pair.",
      "verify": "pnpm style"
    },
    {
      "id": "function.one-job",
      "statement": "A function performs one job its name fully describes.",
      "verify": "judgment"
    },
    {
      "id": "function.blank-line",
      "statement": "Adjacent function declarations are separated by exactly one blank line.",
      "verify": "pnpm style"
    },
    {
      "id": "function.nesting",
      "statement": "Control flow nests at most two levels deep.",
      "verify": "pnpm style"
    },
    {
      "id": "control.guard-else",
      "statement": "Failed prerequisites return early and `else` appears only when both branches are meaningful alternatives.",
      "verify": "judgment"
    },
    {
      "id": "control.closed-switch",
      "statement": "Closed domain variants use an exhaustive switch and extensible catalogs use keyed declarations.",
      "verify": "judgment"
    },
    {
      "id": "control.ternary",
      "statement": "A ternary expresses only one short symmetric choice and never contains another ternary.",
      "verify": "biome ci ."
    },
    {
      "id": "class.tagged-error-only",
      "statement": "The only authored class directly extends `Schema.TaggedError`.",
      "verify": "pnpm style"
    },
    {
      "id": "failure.domain-fields",
      "statement": "Tagged application failures carry domain fields and leave user-facing wording to the CLI presentation boundary.",
      "verify": "judgment"
    },
    {
      "id": "failure.cli-translation",
      "statement": "The CLI translates tagged failures once and maps usage, operation, health, duplicate, and interrupt states to their documented exit codes.",
      "verify": "judgment"
    },
    {
      "id": "comment.index-proof",
      "statement": "Every indexed non-null access carries a bounds proof comment directly above it.",
      "verify": "pnpm style"
    },
    {
      "id": "comment.hidden-intent-only",
      "statement": "Comments explain a constraint or decision that names, types, and structure cannot express.",
      "verify": "judgment"
    },
    {
      "id": "type.schema-owned-runtime",
      "statement": "Data crossing a runtime, persistence, CLI, environment, catalog, or agent-format boundary is defined as Effect Schema first, with its TypeScript type derived.",
      "verify": "pnpm style"
    },
    {
      "id": "type.valid-states",
      "statement": "Domain types encode valid states directly instead of combining flags whose invalid combinations need conditions.",
      "verify": "judgment"
    },
    {
      "id": "type.selective-brand",
      "statement": "Brands are reserved for domain identities whose accidental interchange would be a real defect.",
      "verify": "judgment"
    },
    {
      "id": "type.schema-serialization",
      "statement": "The owning Schema defines decoding, encoding, defaults, and persisted migrations for its boundary value.",
      "verify": "judgment"
    },
    {
      "id": "type.no-unsafe-any",
      "statement": "Authored types use `unknown` at trust boundaries and never use unsafe `any`.",
      "verify": "pnpm style"
    },
    {
      "id": "type.decode-once",
      "statement": "Each application boundary decodes through its owning Schema once and downstream business logic trusts the decoded value.",
      "verify": "judgment"
    },
    {
      "id": "syntax.no-nullish",
      "statement": "Authored TypeScript and JavaScript never use the nullish coalescing operator.",
      "verify": "biome ci ."
    },
    {
      "id": "type.no-interface",
      "statement": "Interfaces appear only for declaration merging or an external interoperability contract that requires one.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-enum",
      "statement": "Unions derived from schema literals replace enums.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-conditional",
      "statement": "Types are derived from schemas, never computed with conditional or `infer` machinery.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-assertion",
      "statement": "Unknown input is decoded or narrowed, while `as const`, `satisfies`, and a documented narrow external-type correction remain allowed.",
      "verify": "pnpm style"
    },
    {
      "id": "type.no-suppression",
      "statement": "A suppression is allowed only for a reasoned negative type test or a narrow external defect linked to an issue.",
      "verify": "pnpm style"
    },
    {
      "id": "module.no-passive-barrel",
      "statement": "Authored modules export their own capability directly and never exist only to re-export sibling modules.",
      "verify": "pnpm style"
    },
    {
      "id": "name.domain-specific",
      "statement": "Every authored identifier and path names its domain job and contains none of the forbidden generic tokens.",
      "verify": "pnpm style"
    },
    {
      "id": "mutation.no-input",
      "statement": "A function never mutates a value it received.",
      "verify": "pnpm style"
    },
    {
      "id": "collection.intent",
      "statement": "Collections use direct transformations, explicit loops for sequential or early-exit work, and `reduce` only for a genuine named aggregation.",
      "verify": "judgment"
    },
    {
      "id": "effect.no-promise-all",
      "statement": "Application code composes concurrency with Effect operators, never `Promise.all`.",
      "verify": "pnpm style"
    },
    {
      "id": "effect.runtime-edge",
      "statement": "Only `src/cli/main.ts` starts the Effect runtime.",
      "verify": "pnpm style"
    },
    {
      "id": "effect.official-services",
      "statement": "Capabilities call official Effect platform services directly.",
      "verify": "judgment"
    },
    {
      "id": "import.hook-runtime",
      "statement": "An installed hook imports only `node:*` builtins, shared `src/runtime/**`, and its own feature runtime.",
      "verify": "pnpm style"
    },
    {
      "id": "import.application-boundary",
      "statement": "Application code reaches the hook island only through a feature `command/` module.",
      "verify": "pnpm style"
    },
    {
      "id": "config.precedence",
      "statement": "Managed configuration combines invocation, environment, file, and Schema default precedence once in that order.",
      "verify": "judgment"
    },
    {
      "id": "path.workspace-ownership",
      "statement": "The CLI resolves the workspace once and each capability owns constructors and containment rules for the paths it writes.",
      "verify": "judgment"
    },
    {
      "id": "process.argv",
      "statement": "Child processes receive an executable and argument array without shell interpolation.",
      "verify": "judgment"
    },
    {
      "id": "presentation.terminal-ui",
      "statement": "Application code returns structured results and leaves terminal output to `TerminalUI`.",
      "verify": "pnpm style"
    },
    {
      "id": "test.behavior",
      "statement": "Tests are colocated, name explicit scenarios, exercise public behavior in real temporary workspaces, and mock only external systems.",
      "verify": "judgment"
    },
    {
      "id": "tooling.tool-first",
      "statement": "Maintained scripts exist only for repository-specific lifecycle work that an installed tool cannot perform directly.",
      "verify": "judgment"
    },
    {
      "id": "tooling.exact-dependencies",
      "statement": "Runtime and development dependencies use exact versions with one lockfile per package lifecycle.",
      "verify": "pnpm install --frozen-lockfile"
    },
    {
      "id": "tooling.git-hooks-verify",
      "statement": "Git hooks run deterministic checks and never rewrite or stage repository files.",
      "verify": "judgment"
    },
    {
      "id": "git.feature-branch",
      "statement": "All product changes land from a named topic branch via a focused PR or branch push, never as direct product commits on the default branch.",
      "verify": "judgment"
    }
  ],
  "protectedPaths": [
    {
      "path": "src/skills/makeATrailer/SKILL.md",
      "codeRuleExemptions": []
    },
    {
      "path": "src/skills/makeATrailer/reference/pipeline.md",
      "codeRuleExemptions": []
    },
    {
      "path": "src/skills/makeATrailer/scripts/assembleCut.mjs",
      "codeRuleExemptions": [
        "function.arrow-only",
        "function.input-shape"
      ]
    }
  ]
}
