# dufflebag — Purpose & Direction

The canonical, detailed project purpose lives at [`templates/mdFiles/PROJECT.md`](templates/mdFiles/PROJECT.md).

That doc is part of the copyable template set (`templates/mdFiles/`) and is therefore also the version that gets scaffolded into other repos by `dufflebag` tooling. This root file exists only to satisfy the standard repo structure convention; edit the canonical copy, and keep this pointer in sync if the path ever changes.
