{
  "$schema": "https://biomejs.dev/schemas/2.5.2/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": {
    "includes": ["src/**/*.ts"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 140
  },
  "assist": {
    "enabled": true,
    "actions": { "source": { "organizeImports": "on" } }
  },
  "javascript": {
    "formatter": { "quoteStyle": "double", "trailingCommas": "all", "semicolons": "always" }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "style": {
        "noNonNullAssertion": "off"
      }
    }
  },
  "plugins": ["./biome-rules/no-focused-tests.grit"],
  "overrides": [
    {
      "includes": ["src/skills/*/hooks/**", "src/skills/*/command/**", "src/runtime/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "commander": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "@clack/prompts": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "picocolors": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "effect": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "@effect/cli": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "@effect/platform": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001).",
                  "@effect/platform-node": "Zero-dep hook/gate runtime: import only node:*, src/runtime/*, and the feature's own lib/ (CODE-STYLE.md -> Never; ADR 0001)."
                }
              }
            }
          }
        }
      }
    }
  ]
}
